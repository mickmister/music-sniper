import React from 'react'
import ReactDOM from 'react-dom'
import Main from './main'
// import Scratch from './scratch'

const root = document.getElementById('main')
// @ts-ignore
// ReactDOM.render(<Scratch />, root)
ReactDOM.render(<Main />, root)
